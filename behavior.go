
func Insert(number int) (bool, bool) {
	if t.check(number) && !t.empty() {

	} else {
		return false, false
	}

}

func Delete(number int) (bool, bool) {
	if t.check(number) && !t.empty() {

	} else {
		return false, false
	}
}

func Inorder() (bool, bool) {
	if !t.empty() {

	} else {
		return false, false
	}
}

func LeverOrder() (bool, bool) {
	if !t.empty() {

	} else {
		return false, false
	}
}
